language: node_js

node_js:
    - "6"

sudo: required

services:
  - docker

env:
  DBUS_SESSION_BUS_ADDRESS=/dev/null

script:
  - npm run lint
  - npm run deps
  - npm run test
  - npm run dev &
  - sleep 2
  - xvfb-run npm run test-e2e
  - pkill node
